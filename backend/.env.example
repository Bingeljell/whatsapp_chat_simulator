# Example Environment Variables for Backend

# Supabase Analytics Configuration (Optional)
# If these variables are not set, the backend will default to
# logging analytics data to a local JSON file (backend/data/analytics.json).
SUPABASE_URL=your_supabase_project_url
SUPABASE_KEY=your_supabase_anon_key
